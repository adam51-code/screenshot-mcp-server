{
  "name": "screenshot-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for capturing web page screenshots via Cloudflare Browser Run + Puppeteer + Stagehand",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "@browserbasehq/stagehand": "2.5.x",
    "@cloudflare/playwright": "^1.2.0",
    "@cloudflare/puppeteer": "^1.3.0",
    "zod": "3.25.67",
    "zod-to-json-schema": "^3.24.6"
  }
}
